# Root is node
# Target = target

# SHOULD BE ITERATIVE
def bfs(root, target)
    
        # create a q
        # add a root to the q >> this should probably be the start

        # remove the first node from the q
        # checj if node.value == target
        # Y: return node
        # N: Add node's children to the q

end

# q = [a]
# target = d

# until q.empty?
# remove, shift, set it to current_node
# current_node = shift? 
# Check if val == target
# Y: 
# N: << the current.children

# return nil if target is never found, when the q.empty?

# Question: How do we shovel? Just the val, or the node?
# The node is an instance of Node class